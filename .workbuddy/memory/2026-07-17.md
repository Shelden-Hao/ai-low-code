# 2026-07-17 工作日志

## FlowAI Studio 项目启动

### 启动方式
- Docker 跑基础设施（PostgreSQL + pgvector + Redis）
- 手动跑开发服务器（后端 NestJS + 前端 Vite）

### 完成步骤
1. ✅ Docker 启动 PostgreSQL + Redis 容器（均健康运行）
2. ✅ 配置后端 .env 环境变量（数据库、Redis、JWT 等）
3. ✅ 安装后端 npm 依赖（使用淘宝镜像加速）
4. ✅ 安装前端 npm 依赖（使用淘宝镜像加速）
5. ✅ 初始化数据库 — Prisma Client 生成 + db push（PostgreSQL 表结构就绪）
6. ✅ 填充种子数据 — admin/admin123 用户、默认知识库、演示应用和工作流
7. ✅ 启动后端（NestJS）— 热重载模式，http://localhost:3000
8. ✅ 启动前端（Vite）— 热重载模式，http://localhost:5173

### 修复的问题
1. **prisma/seed.ts** 中 raw SQL 列名不匹配：PostgreSQL 表使用驼峰列名（chunkIndex, startIndex 等），而非下划线命名，已修复

### 运行服务
- 前端：http://localhost:5173
- 后端 API：http://localhost:3000
- 健康检查：http://localhost:3000/api/health（所有状态正常）
- 默认账号：admin / admin123

### 注意事项
- QWEN_API_KEY 未配置，AI 相关功能（LLM 调用、Embedding）需要设置
- 后端依赖安装时使用了 `--registry=https://registry.npmmirror.com` 淘宝镜像
- 前端使用 Vite 默认端口 5173，后端使用 3000
